server:
  port: 8080

produit:
  ribbon:
    listOfServers: 
      one: "localhost:9001"
      two: "localhost:9011"

commande:
  ribbon:
    listOfServers: 
      one: "localhost:9002"
      two: "localhost:9012"

paiement:
  ribbon:
    listOfServers: 
      one: "localhost:9003"
      two: "localhost:9013"