server:
  port: 8080

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9102/eureka

management:
  endpoints:
    web:
      exposure:
        include: "*"

produit:
  ribbon:
    listOfServers: localhost:9001, localhost:9011

commande:
  ribbon:
    listOfServers: localhost:9002, localhost:9012, localhost:9022

paiement:
  ribbon:
    listOfServers: localhost:9003, localhost:9013