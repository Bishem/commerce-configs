server:
  servlet:
    session:
      timeout: 120s

eureka:
  instance:
    health-check-url-path: "/actuator/health"
  client:
    serviceUrl:
      defaultZone: "http://localhost:9102/eureka"

management:
  endpoints:
    web:
      exposure:
        include: "*"

info:
  app:
    version: 1.0-Beta