#server:
#  port: ${PORT:${SERVER_PORT:0}}

eureka:
  client:
    serviceUrl:
      defaultZone: "http://localhost:9102/eureka"

management:
  endpoints:
    web:
      exposure:
        include: "*"